ktor:
  deployment:
    sslPort: 443
  security:
    ssl:
      keyStore: keystore.jks
      keyAlias: sampleAlias
      keyStorePassword: passpass
      privateKeyPassword: passpass
  application:
    modules:
      - com.example.AppConfigKt.configureHttp
      - com.example.AppConfigKt.configureSerialization
      - com.example.AppConfigKt.configureRouting
